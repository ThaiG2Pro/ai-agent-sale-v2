# =================================================================
# STRATEGIC DEFENSIVE CONFIGURATION - PYTHON TEMPLATE (2025-2026)
# =================================================================

# --- SECRETS & CREDENTIALS (Tối quan trọng) ---
.env
.env.*
.envrc
auth.json
secrets.toml
# Ngoại lệ: Cho phép file ví dụ để team nắm được cấu trúc
!.env.example

# Khóa bảo mật và chứng chỉ
*.pem
*.key
*.pub
*.crt
*.pfx

# --- AI-NATIVE EDITORS & MODERN IDEs ---
.vscode/
.idea/
# Cursor AI editor
.cursor/
# Windsurf AI agent config
.windsurf/
# Google IDX
.idx/
*.swp
.DS_Store

# --- CodeGraph MCP index (local, regenerated via `codegraph init`) ---
.codegraph/

# --- PYTHON ENVIRONMENTS (Rye, Poetry, Venv) ---
.venv/
venv/
env/
.python-version
bin/
lib/
# Ghi chú: Có thể commit poetry.lock/pdm.lock tùy chiến lược của team

# --- AI/ML MODELS & DATA (Loại bỏ tệp lớn) ---
*.h5
*.onnx
*.pt
*.safetensors

# --- BUILD & DISTRIBUTION ---
__pycache__/
*.py[cod]
*$py.class
build/
dist/
*.egg-info/
.installed.cfg
*.egg

# --- TESTING & QUALITY ---
.pytest_cache/
.coverage
htmlcov/
.tox/
.mypy_cache/
.ruff_cache/   # Ruff linter (chuẩn mới 2025)


# my local
mise.toml
# reports/ — intentionally tracked; remove exclusion
GEMINI.md 

.gemini/
.github/
#docs/

.agent/


db_password.txt   
# Secrets directory
secrets/

# >>> kiro-sdlc-kit >>>
# Kit-owned: regenerated by `kiro-sdlc-init --force`. Delete this block to commit the kit.
# (openspec/, docs/, memory/ are intentionally NOT ignored — commit those. Two exceptions:
# memory/*/_index.md is a regenerable digest that collides across parallel branches; /context/
# is ignored by explicit project choice — symlink it into new worktrees, see SKILL.md.)
.claude/
.kiro/
/sdlc.config.json
/pipelines.json
memory/*/_index.md
/context/
# <<< kiro-sdlc-kit <<<
reports/eval_runs/
